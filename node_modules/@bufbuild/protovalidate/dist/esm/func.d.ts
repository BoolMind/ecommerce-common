import { type CelFunc } from "@bufbuild/cel";
export type RegexMatcher = (pattern: string, against: string) => boolean;
export declare function createCustomFuncions(regexMatcher?: RegexMatcher): CelFunc[];
