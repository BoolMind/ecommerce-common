import { type CelResult } from "./error.js";
import { type CelInput, type CelType, type CelValue, type CelValueTuple } from "./type.js";
declare const privateSymbol: unique symbol;
/**
 * Common abstration for CEL functions and methods.
 */
export interface CelFunc {
    [privateSymbol]: unknown;
    /**
     * A unique identifier for the function which is
     * a comibination of name, target, and argument types.
     */
    readonly id: string;
    /**
     * Name of the function/method.
     */
    readonly name: string;
    /**
     * Target type of the function. For example, type of `x` in
     * `x.f()`.
     */
    readonly target: CelType | undefined;
    /**
     * Array of argument types.
     */
    readonly arguments: readonly CelType[];
    /**
     * The result type of function.
     */
    readonly result: CelType;
    /**
     * Calls the function with given arguments and target.
     *
     * Returns undefined if the function's signature doesn't match target/parameters.
     */
    call(id: number, target: CelValue | undefined, args: CelValue[]): CelResult | undefined;
}
/**
 * Creates a new CEL function.
 */
export declare function celFunc<const P extends readonly CelType[], const R extends CelType>(name: string, args: P, result: R, impl: (this: undefined, ...args: CelValueTuple<P>) => CelInput<R>): CelFunc;
/**
 * Creates a new CEL method.
 */
export declare function celMethod<const T extends CelType, const P extends readonly CelType[], const R extends CelType>(name: string, target: T, args: P, result: R, impl: (this: CelValue<T>, ...args: CelValueTuple<P>) => CelInput<R>): CelFunc;
export {};
