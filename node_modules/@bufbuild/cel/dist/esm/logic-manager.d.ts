import type { Expr } from "@bufbuild/cel-spec/cel/expr/syntax_pb.js";
import type Builder from "./builder.js";
export default class LogicManager {
    private builder;
    private function;
    private terms;
    private ops;
    private variadicASTs;
    private constructor();
    static newVariadicLogicManager(builder: Builder, functionName: string, term: Expr): LogicManager;
    static newBalancingLogicManager(builder: Builder, functionName: string, term: Expr): LogicManager;
    addTerm(op: number, term: Expr): void;
    toExpr(): Expr;
    private balancedTree;
}
